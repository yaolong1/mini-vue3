import { NodeTransform } from "../transform"

//转换元素
export const transformText: NodeTransform = (node, context) => {

  //=========进入阶段代码=========//
  return () => {
    //=========退出阶段代码=========//
  }
}
